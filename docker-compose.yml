services:
  claude-context:
    build: .
    container_name: claude-context
    ports:
      - "127.0.0.1:4100:4100"
    volumes:
      - ~/.claude-context:/data
    restart: unless-stopped
    environment:
      - PORT=4100
      - DB_PATH=/data/db.sqlite
